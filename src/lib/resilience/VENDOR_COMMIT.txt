Vendored-copy pin for the cubiczan-resilience TypeScript package
================================================================

Upstream package : @cubiczan/resilience (TypeScript)
Upstream repo    : https://github.com/icohangar-ops/cubiczan-resilience
Pinned version   : typescript-v0.2.0
Pinned commit    : 60dc5f4b7030bef492fe5df0d432ae49fd612f37
Pin date         : 2026-09-20
Sync status      : keep-in-sync — this directory is a vendored copy, not the package

Scope of this copy
------------------
auth.ts, errors.ts, index.ts, rateLimit.ts, retry.ts, safeFetch.ts,
timeout.ts (most of the canonical package's modules).

Intentional local deltas (preserve these when syncing — do not "fix" them
by copying canonical files over this directory wholesale)
- Import extensions are stripped (./errors, not ./errors.js) to
  match this repo's module style; vendored-header comments carry
  the note inline.
- Some JSDoc provenance prose is trimmed vs upstream.
- Does not yet include the typescript-v0.2.0 clock fix
  (SlidingWindowRateLimiter.nowMs / retryAfterMs on the limiter's
  clock); it lands on the next sync.

Missing from this copy (relative to the pinned upstream)
--------------------------------------------------------
Anything not listed under "Scope" above, including canonical modules added
after this copy was vendored (e.g. the typescript-v0.2.0 proxyGuard) unless
noted as present.

Refresh procedure
-----------------
1. Diff this directory against cubiczan-resilience/typescript/src at the
   pinned commit, applying the intentional local deltas above.
2. Run this repo's lint/typecheck/tests.
3. Update the pinned version/commit here and in each vendored file's header.

What typescript-v0.2.0 added upstream
-------------------------------------
- proxyGuard (checkProxyRequest / guardProxyRequest): fail-closed proxy-route
  guard — caller-secret header + per-IP sliding-window rate limit.
- SlidingWindowRateLimiter.nowMs(): consumers (auth, proxyGuard) measure
  retryAfterMs on the limiter's own clock instead of Date.now.
